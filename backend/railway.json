{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c \"export ENTRYPOINT=$(find /app -name main.js | grep dist | head -n 1); node $ENTRYPOINT\"",
    "restartPolicyType": "ON_FAILURE"
  }
}
